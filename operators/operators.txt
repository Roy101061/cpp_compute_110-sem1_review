運算元/運算子

+ - * /

% → 取餘數
++ → +1
-- → -1
= → 指定

> → 大於
< → 小於
>= → 大於等於
<= → 小於等於
!= → 不等於
== → 判斷是否等於

a+=b → a=a+b
a-=b → a=a-b
a*=b → a=a*b
a/=b → a=a/b
a%=b → a=a%b
a&=b → a=a&b
a|=b → a=a|b
a^=b → a=a^b
a<<=b → a=a<<b
a>>=b → a=>>b

! → 非(是變否,否變是)
&& → 且
|| → 或

(條件) ? (成立時) : (不成立時)  
